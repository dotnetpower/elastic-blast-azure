apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blast-dbs-pvc-rwm
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azure-netapp-ultra
  resources:
    requests:
      storage: ${ELB_PD_SIZE}
